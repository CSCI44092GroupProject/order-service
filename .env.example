# Copy this file to .env and fill in real values. .env is gitignored.
# IMPORTANT: If your password contains '#' or '$', wrap it in single quotes.

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=order_db
DB_USERNAME=order_user
DB_PASSWORD='changethis'
